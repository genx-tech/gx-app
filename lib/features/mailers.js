"use strict";const Feature=require("../enum/Feature");const{tryRequire}=require("../utils/Helpers");const{_}=require("rk-utils");module.exports={type:Feature.SERVICE,load_:async function(app,mailers){const nodemailer=tryRequire("nodemailer");_.forOwn(mailers,(options,name)=>{let transporter=nodemailer.createTransport(options);app.registerService(`mailers.${name}`,transporter)})}};